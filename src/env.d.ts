/**
 * Déclarations de types pour l'environnement Vite.
 *
 * La référence `vite/client` ajoute les types pour :
 *  - `import.meta.env` (BASE_URL, MODE, DEV, PROD, SSR + variables VITE_*)
 *  - Les imports d'assets (images, CSS, JSON, etc.)
 *  - Les modules HMR (`import.meta.hot`)
 */

/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/client" />
